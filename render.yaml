services:
  - type: web
    name: asistente-mayores-backend
    env: python
    repo: https://github.com/mrezk84/AsistenteMayoresAI
    rootDir: asistente-backend
    region: ohio
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: GEMINI_API_KEY
        sync: false # Must be set in Render dashboard
      - key: API_SECRET_TOKEN
        value: "asistente-mayores-token-2024"
      - key: JWT_SECRET
        sync: false # Must be set in Render dashboard - use a strong secret in production
